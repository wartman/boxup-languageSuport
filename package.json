{
  "name": "boxup-ls",
  "publisher": "",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.4.0"
  },
  "main": "dist/extension.js",
  "activationEvents": [
    "onLanguage:box",
    "onCommand:box.compile"
  ],
  "contributes": {
    "commands": [
      {
        "title": "Compile using .boxconfig",
        "category": "Boxup",
        "command": "box.compile"
      }
    ]
  },
  "dependencies": {
    "pdfkit": "^0.12.0"
  }
}
